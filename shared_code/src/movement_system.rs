//Ordering
//Frame Rune -> which takes in world and outputs world + input itentions??
//We then want to clear and reapply buffs?? 
//well, yeah we need a layering system of some kind


//Would be nice if actually all it took was, transform_components, attributes_components, status_components
fn movement_system(mut world: World, mut events: Vec<Box<Rune>>) -> (World, Vec<Box<Rune>>) {

}